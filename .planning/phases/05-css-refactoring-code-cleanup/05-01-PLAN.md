---
phase: 05-css-refactoring-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/App.css
  - client/src/index.css
  - client/src/App.tsx
  - client/src/components/BillCard.tsx
  - client/src/components/BillDashboard.tsx
  - client/src/components/BillEditModal.tsx
  - client/src/components/BillUploader.tsx
  - client/src/components/SpendingOverview.tsx
  - client/src/components/AppHeader.css
  - client/src/components/AuthPage.css
  - client/src/components/BillDashboard.css
  - client/src/components/Sidebar.css
  - client/src/components/BillUploader.css
  - client/src/components/CategoryTabs.css
  - client/src/components/BillCard.css
  - client/src/components/Skeleton.css
  - client/src/components/Toast.css
  - client/src/components/ImageViewer.css
  - client/src/components/ErrorBoundary.css
  - client/src/components/BillEditModal.css
  - client/src/components/AnalyticsPage.css
  - client/src/components/UpcomingBills.css
  - client/src/components/SettingsPage.css
  - client/src/components/MobileNav.css
  - server/src/models/Bill.js
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03
  - CLEANUP-04
  - CLEANUP-05
must_haves:
  truths:
    - Each component has its own CSS file imported via `import './ComponentName.css'`
    - App.css no longer exists (all styles distributed to per-component files)
    - No TSX file contains inline `style={{ }}` for static CSS properties
    - Dynamic inline styles use CSS custom properties (e.g. `style={{ '--category-color': val }}`)
    - All hardcoded hex colors in dark mode replaced with CSS custom properties
    - Bill model has compound indexes {userId, createdAt} and {userId, category}
    - `npm run build` in client/ passes with zero CSS import errors
    - Visual appearance is identical before and after (no specificity regressions)
  artifacts:
    - client/src/components/AppHeader.css
    - client/src/components/AuthPage.css
    - client/src/components/BillDashboard.css
    - client/src/components/Sidebar.css
    - client/src/components/BillUploader.css
    - client/src/components/CategoryTabs.css
    - client/src/components/BillCard.css
    - client/src/components/Skeleton.css
    - client/src/components/Toast.css
    - client/src/components/ImageViewer.css
    - client/src/components/ErrorBoundary.css
    - client/src/components/BillEditModal.css
    - client/src/components/AnalyticsPage.css
    - client/src/components/UpcomingBills.css
    - client/src/components/SettingsPage.css
    - client/src/components/MobileNav.css
    - server/src/models/Bill.js (with compound indexes)
  key_links:
    - App.css line ranges map exactly to per-component files (no styles lost)
    - CSS import order in TSX matches the original App.css specificity order
    - CSS custom properties flow from index.css :root through component files
    - Bill compound indexes match query patterns in billController.js
---

<objective>
Refactor the monolithic 2773-line App.css into per-component CSS files, convert all inline styles to CSS classes or CSS custom properties, add dark mode CSS tokens for hardcoded colors, and add compound database indexes to the Bill model.

Purpose: Establish a maintainable CSS architecture where each component owns its styles, eliminate inline styling anti-patterns, complete the dark mode token system, and optimize the two most common database query patterns.

Output: ~16 per-component CSS files, updated TSX imports, zero inline styles for static properties, CSS custom property usage for dynamic values, new dark mode tokens in index.css, compound indexes in Bill.js, passing build.
</objective>

<execution_context>
@/home/vim/videcode/pyat-paing/.opencode/gsd-core/workflows/execute-plan.md
@/home/vim/videcode/pyat-paing/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-css-refactoring-code-cleanup/05-RESEARCH.md
@.planning/phases/05-css-refactoring-code-cleanup/05-UI-SPEC.md
@client/src/App.css
@client/src/index.css
@client/src/App.tsx
@client/src/components/BillCard.tsx
@client/src/components/BillDashboard.tsx
@client/src/components/BillEditModal.tsx
@client/src/components/BillUploader.tsx
@client/src/components/SpendingOverview.tsx
@server/src/models/Bill.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split App.css into per-component CSS files and add imports to TSX</name>
  <files>
    client/src/App.css (DELETE after split),
    client/src/components/AppHeader.css (CREATE),
    client/src/components/AuthPage.css (CREATE),
    client/src/components/BillDashboard.css (CREATE),
    client/src/components/Sidebar.css (CREATE),
    client/src/components/BillUploader.css (CREATE),
    client/src/components/CategoryTabs.css (CREATE),
    client/src/components/BillCard.css (CREATE),
    client/src/components/Skeleton.css (CREATE),
    client/src/components/Toast.css (CREATE),
    client/src/components/ImageViewer.css (CREATE),
    client/src/components/ErrorBoundary.css (CREATE),
    client/src/components/BillEditModal.css (CREATE),
    client/src/components/AnalyticsPage.css (CREATE),
    client/src/components/UpcomingBills.css (CREATE),
    client/src/components/SettingsPage.css (CREATE),
    client/src/components/MobileNav.css (CREATE),
    client/src/App.tsx (MODIFY — remove App.css import),
    client/src/components/BillCard.tsx (MODIFY — add import),
    client/src/components/BillDashboard.tsx (MODIFY — add import),
    client/src/components/BillEditModal.tsx (MODIFY — add import),
    client/src/components/BillUploader.tsx (MODIFY — add import),
    client/src/components/SpendingOverview.tsx (MODIFY — add import),
    client/src/components/AuthPage.tsx (MODIFY — add import),
    client/src/components/Sidebar.tsx (MODIFY — add import),
    client/src/components/CategoryTabs.tsx (MODIFY — add import),
    client/src/components/Toast.tsx (MODIFY — add import),
    client/src/components/ErrorBoundary.tsx (MODIFY — add import),
    client/src/components/AnalyticsPage.tsx (MODIFY — add import),
    client/src/components/UpcomingBills.tsx (MODIFY — add import),
    client/src/components/SettingsPage.tsx (MODIFY — add import),
    client/src/components/ProfilePage.tsx (MODIFY — add import),
    client/src/components/MobileNav.tsx (MODIFY — add import)
  </files>
  <read_first>
    client/src/App.css (full file — read all 2773 lines to understand the exact line ranges before splitting)
    client/src/index.css (design tokens — verify no base styles are duplicated)
    client/src/App.tsx (verify current `import './App.css'` at line 18)
    client/src/components/BillCard.tsx (verify component structure)
    client/src/components/BillDashboard.tsx (verify component structure)
    client/src/components/BillEditModal.tsx (verify component structure)
    client/src/components/BillUploader.tsx (verify component structure)
    client/src/components/SpendingOverview.tsx (verify component structure)
    client/src/components/AuthPage.tsx (verify component structure)
    client/src/components/Sidebar.tsx (verify component structure)
    client/src/components/CategoryTabs.tsx (verify component structure)
    client/src/components/Toast.tsx (verify component structure)
    client/src/components/ErrorBoundary.tsx (verify component structure)
    client/src/components/AnalyticsPage.tsx (verify component structure)
    client/src/components/UpcomingBills.tsx (verify component structure)
    client/src/components/SettingsPage.tsx (verify component structure)
    client/src/components/ProfilePage.tsx (verify component structure)
    client/src/components/MobileNav.tsx (verify component structure)
  </read_first>
  <action>
Split the monolithic App.css (2773 lines) into per-component CSS files. Each file is created with `import './ComponentName.css'` added to its corresponding TSX component. This is per CLEANUP-01.

**Step 1 — Read App.css fully.** Before creating any file, read all 2773 lines to confirm the exact line ranges. The ranges below are from research; verify they match the actual file content.

**Step 2 — Create per-component CSS files using these line ranges from App.css:**

| Target CSS File | App.css Lines | TSX Component |
|----------------|---------------|---------------|
| `components/AppHeader.css` | 6–105 + 2159–2239 | App.tsx (header/nav/theme) |
| `components/AuthPage.css` | 107–290 | AuthPage.tsx |
| `components/BillDashboard.css` | 292–306 + 625–650 + 697–782 + 1025–1113 | BillDashboard.tsx |
| `components/Sidebar.css` | 307–453 | Sidebar.tsx |
| `components/BillUploader.css` | 454–624 | BillUploader.tsx |
| `components/CategoryTabs.css` | 651–696 | CategoryTabs.tsx |
| `components/BillCard.css` | 783–966 + 1982–2047 | BillCard.tsx |
| `components/Skeleton.css` | 967–1024 | (shared — imported by BillDashboard.tsx) |
| `components/Toast.css` | 1114–1193 | Toast.tsx |
| `components/ImageViewer.css` | 1379–1462 | BillCard.tsx (lightbox rendered in BillCard) |
| `components/ErrorBoundary.css` | 1463–1549 | ErrorBoundary.tsx |
| `components/BillEditModal.css` | 1550–1720 + 2371–2508 (modal recurring section) | BillEditModal.tsx |
| `components/AnalyticsPage.css` | 1751–1980 | AnalyticsPage.tsx |
| `components/UpcomingBills.css` | 2049–2157 | UpcomingBills.tsx |
| `components/SettingsPage.css` | 2240–2370 + 2371–2508 (settings/export sections) | SettingsPage.tsx, ProfilePage.tsx |
| `components/MobileNav.css` | 2565–2773 | MobileNav.tsx |

**Step 3 — Handle responsive overrides.** Lines 1194–1378 (responsive tablet/mobile) and 1721–1750 (responsive large) and 2510–2563 (mobile responsive) contain media queries that target multiple component selectors. Distribute each `@media` block to the component file that owns its selectors. For example, a `@media (max-width: 640px) { .bill-card__image-wrap { ... } }` block goes into `BillCard.css`. If a media query targets selectors from multiple components, split the block — each component gets its own `@media` block at the end of its file.

**Step 4 — Add CSS imports to TSX files.** Each TSX component gets `import './ComponentName.css';` as the LAST import statement (after all other imports). The specific imports to add:

- `App.tsx`: REMOVE `import './App.css';` (line 18) — App.tsx no longer imports any CSS. Header styles are in AppHeader.css imported by App.tsx. Actually, App.tsx still needs `import './components/AppHeader.css';` since the header is rendered in App.tsx. Add it as the last import.
- `AuthPage.tsx`: ADD `import './AuthPage.css';`
- `BillDashboard.tsx`: ADD `import './BillDashboard.css';` AND `import './Skeleton.css';` (Skeleton is shared)
- `Sidebar.tsx`: ADD `import './Sidebar.css';`
- `BillUploader.tsx`: ADD `import './BillUploader.css';`
- `CategoryTabs.tsx`: ADD `import './CategoryTabs.css';`
- `BillCard.tsx`: ADD `import './BillCard.css';` AND `import './ImageViewer.css';` (lightbox is rendered in BillCard)
- `Toast.tsx`: ADD `import './Toast.css';`
- `ErrorBoundary.tsx`: ADD `import './ErrorBoundary.css';`
- `BillEditModal.tsx`: ADD `import './BillEditModal.css';`
- `AnalyticsPage.tsx`: ADD `import './AnalyticsPage.css';`
- `UpcomingBills.tsx`: ADD `import './UpcomingBills.css';`
- `SettingsPage.tsx`: ADD `import './SettingsPage.css';`
- `ProfilePage.tsx`: ADD `import './SettingsPage.css';` (shares styles with SettingsPage)
- `MobileNav.tsx`: ADD `import './MobileNav.css';`

**Step 5 — Delete App.css.** After all lines are distributed and imports added, delete `client/src/App.css`. Verify no remaining imports reference it.

**CRITICAL rules:**
- Copy CSS text EXACTLY — do not modify selectors, properties, or values during the split. This is a mechanical extraction, not a rewrite.
- Maintain the original source order within each component file. If App.css has `.bill-card` at line 783 and responsive `.bill-card` at line 1291, the component file must have them in that same order.
- Each component file ends with its own `@media` blocks and dark mode `[data-theme="dark"]` overrides.
- Do NOT create CSS Modules (*.module.css). All files remain global CSS.
  </action>
  <verify>
    <automated>cd client && npm run build 2>&1 | tail -5</automated>
    <human-check>Verify no `import './App.css'` remains in any TSX file (grep for it). Verify App.css file no longer exists. Verify each component CSS file exists and is non-empty. Verify build passes with no errors.</human-check>
  </verify>
  <acceptance_criteria>
    - [ ] App.css no longer exists at client/src/App.css
    - [ ] 16 per-component CSS files exist in client/src/components/
    - [ ] Each TSX component imports its own CSS file (grep for `import './` in each TSX)
    - [ ] App.tsx no longer has `import './App.css'`
    - [ ] `cd client && npm run build` succeeds with zero errors
    - [ ] Total CSS content across all new files equals the original App.css content (no styles lost)
  </acceptance_criteria>
  <done>App.css is split into 16 per-component CSS files. Each TSX component imports its own styles. Build passes. No styles are lost.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline styles with CSS classes/variables and fix dark mode hardcoded colors</name>
  <files>
    client/src/components/BillCard.tsx (MODIFY — replace inline backgroundColor),
    client/src/components/BillCard.css (MODIFY — add --category-color variable),
    client/src/components/BillDashboard.tsx (MODIFY — replace 3 inline styles),
    client/src/components/BillDashboard.css (MODIFY — add 3 new classes),
    client/src/components/BillEditModal.tsx (MODIFY — replace inline borderColor/backgroundColor),
    client/src/components/BillEditModal.css (MODIFY — add --cat-color variable),
    client/src/components/BillUploader.tsx (MODIFY — replace inline color/fontSize),
    client/src/components/BillUploader.css (MODIFY — add .uploader__hint class),
    client/src/components/SpendingOverview.tsx (MODIFY — replace 3 inline styles),
    client/src/components/Skeleton.css (MODIFY — add .skeleton--chart class),
    client/src/components/BillDashboard.css (MODIFY — add .budget-alert classes),
    client/src/components/UpcomingBills.css (MODIFY — replace hardcoded dark mode hex colors),
    client/src/index.css (MODIFY — add new CSS tokens),
    client/src/components/AppHeader.css (MODIFY — add .app-title--clickable)
  </files>
  <read_first>
    client/src/components/BillCard.tsx (lines 95-105 — inline backgroundColor at line 99)
    client/src/components/BillDashboard.tsx (lines 14-27 — skeleton inline at line 18; lines 198-210 — inline at lines 200 and 204)
    client/src/components/BillEditModal.tsx (lines 96-112 — inline borderColor/backgroundColor at lines 102-105)
    client/src/components/BillUploader.tsx (lines 188-198 — inline color/fontSize at line 194)
    client/src/components/SpendingOverview.tsx (lines 49-59 — inline height at line 56; lines 121-136 — inline background at line 126, color at line 132)
    client/src/components/BillCard.css (verify existing styles for .bill-card__category)
    client/src/components/BillDashboard.css (verify existing .dashboard__summary and .skeleton-card styles)
    client/src/components/BillEditModal.css (verify existing .modal-form__category-btn styles)
    client/src/components/BillUploader.css (verify existing .bill-uploader__selected styles)
    client/src/components/UpcomingBills.css (lines with hardcoded #fef2f2, #fecaca, #fffbeb, #fde68a, #450a0a, #dc2626, #451a03, #f59e0b)
    client/src/index.css (verify :root and [data-theme="dark"] blocks for adding new tokens)
    client/src/components/AppHeader.css (verify .app-title styles)
  </read_first>
  <action>
Convert all inline styles to CSS classes or CSS custom properties, and replace hardcoded dark mode colors with CSS tokens. This covers CLEANUP-02, CLEANUP-03, CLEANUP-04, CLEANUP-05.

**Part A — Inline Style Conversions (CLEANUP-02):**

**1. BillCard.tsx line 99** — Dynamic category color:
- In BillCard.tsx, change `style={{ backgroundColor: categoryColor }}` to `style={{ '--category-color': categoryColor } as React.CSSProperties}`
- In BillCard.css, update `.bill-card__category` to use `background: var(--category-color);` (replace any existing `background-color` property)

**2. BillDashboard.tsx line 18** — Skeleton flex layout:
- In BillDashboard.tsx, change `style={{ display: 'flex', justifyContent: 'space-between' }}` to `className="skeleton-card__header"`
- In BillDashboard.css, add: `.skeleton-card__header { display: flex; justify-content: space-between; }`

**3. BillDashboard.tsx line 200** — Summary flush margin:
- In BillDashboard.tsx, change `style={{ marginBottom: 0 }}` to add ` dashboard__summary--flush` to the existing className (append to `className="dashboard__summary"`)
- In BillDashboard.css, add: `.dashboard__summary--flush { margin-bottom: 0; }`

**4. BillDashboard.tsx line 204** — Summary meta text:
- In BillDashboard.tsx, change `style={{ fontWeight: 400, color: 'var(--color-text-muted)', fontSize: 'var(--text-sm)', marginLeft: 'var(--space-2)' }}` to `className="dashboard__summary-meta"`
- In BillDashboard.css, add:
  `.dashboard__summary-meta { font-weight: var(--font-weight-normal); color: var(--color-text-muted); font-size: var(--text-sm); margin-left: var(--space-2); }`

**5. BillEditModal.tsx lines 102-105** — Dynamic category button color:
- In BillEditModal.tsx, change the `style={{ borderColor: ..., backgroundColor: ... }}` to `style={{ '--cat-color': CATEGORY_COLORS[cat] } as React.CSSProperties}`
- In BillEditModal.css, update `.modal-form__category-btn--active` to use:
  `border-color: var(--cat-color); background-color: color-mix(in srgb, var(--cat-color) 12%, transparent);`
- Note: Remove the existing inline conditional for borderColor and backgroundColor entirely — the CSS variable handles it via the --active modifier class.

**6. BillUploader.tsx line 194** — File size hint text:
- In BillUploader.tsx, change `style={{ color: 'var(--color-text-muted)', fontSize: 'var(--text-xs)' }}` to `className="uploader__hint"`
- In BillUploader.css, add: `.uploader__hint { color: var(--color-text-muted); font-size: var(--text-xs); }`

**7. SpendingOverview.tsx line 56** — Skeleton chart height:
- In SpendingOverview.tsx, change `style={{ height: 200 }}` to `className="skeleton skeleton--chart"`
- In Skeleton.css (or a new spending-overview section in a shared file), add: `.skeleton--chart { height: 200px; }`

**8. SpendingOverview.tsx lines 126-130** — Budget alert bar gradient:
- In SpendingOverview.tsx, change `style={{ background: \`linear-gradient(to right, ${barColor} ${pct}%...)\` }}` to `style={{ '--bar-color': barColor, '--bar-pct': \`${pct}%\` } as React.CSSProperties}`
- Also update the `budget-alert__fill` div's className to include the style variable.
- In BillDashboard.css (or a spending-overview section), add:
  `.budget-alert__fill { background: linear-gradient(to right, var(--bar-color) var(--bar-pct), transparent var(--bar-pct)); }`

**9. SpendingOverview.tsx line 132** — Budget alert label color:
- In SpendingOverview.tsx, change `style={{ color: barColor }}` to remove the inline style (the CSS variable from step 8 propagates).
- In BillDashboard.css, add: `.budget-alert__label { color: var(--bar-color); }`
- Note: The `--bar-color` variable is set on the parent `.budget-alert` div, so both `.budget-alert__fill` and `.budget-alert__label` inherit it.

**10. App.tsx line 74** — Cursor pointer on title:
- In App.tsx, change `style={{ cursor: 'pointer' }}` to `className="app-title app-title--clickable"`
- In AppHeader.css, add: `.app-title--clickable { cursor: pointer; }`

**Part B — Dark Mode Token Cleanup (CLEANUP-05):**

**1. Add new tokens to index.css:**
- In `:root` block, add: `--color-on-accent: #fff;`
- In `[data-theme="dark"]` block, add:
  `--color-on-accent: #fff;`
  `--color-danger-dark: #450a0a;`
  `--color-danger-border-dark: #dc2626;`
  `--color-warning-dark: #451a03;`

**2. Replace hardcoded colors in UpcomingBills.css (formerly App.css lines 2104-2157):**

Find these exact rules and replace hardcoded values:

- `.upcoming-bills__item--overdue`: Replace `background: #fef2f2` with `background: var(--color-danger-light)`. Replace `border: 1px solid #fecaca` with `border: 1px solid var(--color-danger-light)`.
- `.upcoming-bills__item--today`: Replace `background: #fffbeb` with `background: var(--color-warning-light)`. Replace `border: 1px solid #fde68a` with `border: 1px solid var(--color-warning-light)`.
- `[data-theme="dark"] .upcoming-bills__item--overdue`: Replace `background: #450a0a` with `background: var(--color-danger-dark)`. Replace `border-color: #dc2626` with `border-color: var(--color-danger-border-dark)`.
- `[data-theme="dark"] .upcoming-bills__item--today`: Replace `background: #451a03` with `background: var(--color-warning-dark)`. Replace `border-color: #f59e0b` with `border-color: var(--color-warning)` (already exists).
- `.upcoming-bills__item--overdue .upcoming-bills__days`: Replace `color: #dc2626` with `color: var(--color-danger)` (already exists).

**Part C — Database Indexes (CLEANUP-03, CLEANUP-04):**

**1. Add compound indexes to Bill model (server/src/models/Bill.js):**
- After the closing of `billSchema` (line 70, after `{ timestamps: true }`) and before `const Bill = model(...)`, add:
  `billSchema.index({ userId: 1, createdAt: -1 });`
  `billSchema.index({ userId: 1, category: 1 });`

**2. Verify recurringService.js (CLEANUP-04):**
- Read server/src/utils/recurringService.js and confirm it queries all recurring bills at line 13 without userId filter (this is correct for a system-wide cron job). No code change needed. Add a brief comment at line 13: `// System-wide query — processes all users' recurring bills`

**CRITICAL rules:**
- For dynamic inline styles (BillCard category color, BillEditModal category buttons, SpendingOverview budget bars): use CSS custom properties via `style={{ '--var-name': value } as React.CSSProperties}`. The CSS file then uses `var(--var-name)`.
- For static inline styles (BillDashboard skeleton, summary flush, summary meta, BillUploader hint, SpendingOverview skeleton height): create a CSS class and apply it via `className`.
- Do NOT change any visual appearance. The conversion must produce pixel-identical results.
- BEM naming: new classes follow `block__element--modifier` pattern.
  </action>
  <verify>
    <automated>cd client && npm run build 2>&1 | tail -5</automated>
    <automated>grep -rn 'style={{' client/src/components/ | grep -v 'React.CSSProperties' | grep -v 'node_modules' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Zero static inline styles remain in TSX files (all converted to CSS classes)
    - [ ] Dynamic inline styles use CSS custom properties with `React.CSSProperties` type assertion
    - [ ] All 9 hardcoded hex colors in UpcomingBills dark mode replaced with CSS variables
    - [ ] New tokens (`--color-danger-dark`, `--color-danger-border-dark`, `--color-warning-dark`, `--color-on-accent`) exist in index.css
    - [ ] Bill.js has `billSchema.index({ userId: 1, createdAt: -1 })` and `billSchema.index({ userId: 1, category: 1 })`
    - [ ] `cd client && npm run build` succeeds with zero errors
    - [ ] No `#fef2f2`, `#fecaca`, `#fffbeb`, `#fde68a`, `#450a0a`, `#dc2626`, `#451a03`, `#f59e0b` remain in UpcomingBills.css (grep confirms zero matches)
  </acceptance_criteria>
  <done>All inline styles converted to CSS classes or CSS custom properties. Dark mode uses CSS variables exclusively. Bill model has compound indexes. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS specificity | Splitting App.css into multiple files changes load order — must maintain original order to prevent specificity regressions |
| Dark mode tokens | New tokens must be defined in both :root and [data-theme="dark"] to prevent fallback to hardcoded values |
| Database indexes | Index creation on existing collection may take time — non-blocking for reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | CSS load order | medium | mitigate | Maintain exact source order from App.css when distributing to per-component files |
| T-05-02 | Information Disclosure | Hardcoded colors in dark mode | low | mitigate | Replace all 9 hardcoded hex values with CSS custom properties |
| T-05-03 | Denial of Service | Bill model index creation | low | accept | Compound indexes are lightweight; creation is non-blocking |
| T-05-04 | Elevation of Privilege | CSS specificity regression | medium | mitigate | Build verification after each task; visual check in browser |
</threat_model>

<verification>
## Build Verification
```bash
cd client && npm run build
```
Must succeed with zero errors after each task.

## CSS Integrity Check
```bash
# Verify no App.css import remains
grep -rn "import './App.css'" client/src/
# Expected: 0 results

# Verify App.css is deleted
ls client/src/App.css 2>/dev/null
# Expected: file not found

# Verify each component CSS exists
for f in AppHeader AuthPage BillDashboard Sidebar BillUploader CategoryTabs BillCard Skeleton Toast ImageViewer ErrorBoundary BillEditModal AnalyticsPage UpcomingBills SettingsPage MobileNav; do
  test -f "client/src/components/${f}.css" && echo "OK: ${f}.css" || echo "MISSING: ${f}.css"
done
```

## Inline Style Check
```bash
# Remaining inline styles should only be CSS variable assignments (dynamic values)
grep -rn 'style={{' client/src/components/ | grep -v 'CSSProperties' | grep -v 'node_modules'
# Expected: 0 results (all remaining use CSSProperties type assertion)
```

## Dark Mode Check
```bash
# No hardcoded hex in dark mode selectors
grep -n '#fef2f2\|#fecaca\|#fffbeb\|#fde68a\|#450a0a\|#dc2626\|#451a03\|#f59e0b' client/src/components/UpcomingBills.css
# Expected: 0 results
```

## Database Index Check
```bash
grep -n 'billSchema.index' server/src/models/Bill.js
# Expected: 2 lines with compound indexes
```
</verification>

<success_criteria>
1. App.css no longer exists — all 2773 lines distributed to 16 per-component CSS files
2. Zero static inline styles in TSX files — all converted to CSS classes
3. Dynamic inline styles use CSS custom properties with type assertion
4. 9 hardcoded dark mode hex colors replaced with CSS variables
5. New dark mode tokens added to index.css (:root and [data-theme="dark"])
6. Bill model has 2 compound indexes: {userId, createdAt} and {userId, category}
7. recurringService.js verified correct (no changes needed)
8. `cd client && npm run build` passes with zero errors
9. Visual appearance identical before and after refactoring
</success_criteria>

<output>
Create `.planning/phases/05-css-refactoring-code-cleanup/05-01-SUMMARY.md` when done
</output>
